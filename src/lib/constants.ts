/** Values shared by the server and the browser. Nothing sensitive. */

export const APP_NAME = 'AI Document Assistant';
export const APP_VERSION = '0.1.0';

/** Bumped as each phase lands, so the health check reports real progress. */
export const CURRENT_PHASE = 4;
export const CURRENT_PHASE_NAME = 'Google authentication and hosted generation';
