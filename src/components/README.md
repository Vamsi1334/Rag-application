# Components

- `ui/` — primitives (buttons, inputs, status pills)
- `documents/` — upload dropzone, document list, processing status
- `chat/` — message stream, citation panel

Server Components by default. A component becomes a Client Component only when
it needs state, effects or event handlers.
