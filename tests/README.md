# Tests

- `unit/` — pure functions, no I/O, milliseconds
- `integration/` — real database, repositories and tenant isolation (phase 3)
- `e2e/` — full browser flows (phase 14)
- `fixtures/` — small real documents, including awkward ones

Run with `npm test`.
